replicaCount: 1

image:
  repository: argocd-demo
  pullPolicy: IfNotPresent
  tag: "1.0.0"

serviceAccount:
  create: true
  automount: true

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

resources: {}

podSecurityContext: {}

securityContext: {}
